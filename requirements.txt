# --- socle (obligatoire) ---------------------------------------------------
requests==2.32.3
PyYAML==6.0.2
python-dotenv==1.0.1
Pillow==10.4.0

# --- optionnel : voix off gratuite (recommandé) ----------------------------
edge-tts==6.1.12

# --- développement ---------------------------------------------------------
pytest==8.3.3

# Outil externe requis pour le montage vidéo (pas un paquet Python) :
#   Linux  : sudo apt install ffmpeg
#   macOS  : brew install ffmpeg
#   Windows: winget install Gyan.FFmpeg
