# --- socle (obligatoire) ---------------------------------------------------
# Fourchettes plutôt que versions figées : évite les conflits avec les
# paquets déjà installés par le système.
requests>=2.31,<3
PyYAML>=6.0,<7
python-dotenv>=1.0,<2
Pillow>=10.0,<12

# --- optionnel : voix off gratuite (recommandé) ----------------------------
edge-tts>=6.1

# --- développement ---------------------------------------------------------
pytest>=8.0

# Outil externe requis pour le montage vidéo (pas un paquet Python) :
#   Linux  : sudo apt install ffmpeg
#   macOS  : brew install ffmpeg
#   Windows: winget install Gyan.FFmpeg
