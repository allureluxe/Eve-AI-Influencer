# ==========================================================================
#  Luna — compagne virtuelle IA (messages, appel, visio)
# ==========================================================================
# Aucune dependance : Python 3.11+ et un navigateur suffisent. Le serveur
# est en http.server, les appels API en urllib, la voix et l'avatar sont
# dans le navigateur.
#
#   python3 luna.py app

# ==========================================================================
#  Eve AI — bot d'influence (Instagram / TikTok)
# ==========================================================================
instagrapi==2.0.0
TikTokApi==1.0.0
python-dotenv==1.0.0
requests==2.31.0
Pillow==10.0.0
aiohttp==3.8.5
Schedule==1.2.0
OpenAI==1.3.0
stability-sdk==0.7.1
aiofiles==23.2.1
pydantic==2.3.0

# ==========================================================================
#  Robot de trading
# ==========================================================================
# Aucune dependance : le robot n'utilise que la librairie standard de
# Python 3.11+ (urllib, json, dataclasses, deque...). C'est un choix
# deliberé — il tourne sur n'importe quel VPS, demarre en une seconde, et
# rien ne peut casser a cause d'une mise a jour de dependance pendant qu'il
# gere une position ouverte.
#
#   python3 run_bot.py check

# ------------------------------------------------------------------------
#  OPTIONNEL -- notifications push vers Alluxe Bot (FirebasePushChannel)
# ------------------------------------------------------------------------
# Le seul ecart au "aucune dependance" ci-dessus, et delibirement isole :
# gold_bot/notifiers.py importe `jwt` UNIQUEMENT a l'interieur de
# FirebasePushChannel._jeton_oauth(), jamais au sommet du fichier. Si ce
# paquet manque, ce seul canal de notification reste indisponible (log
# d'avertissement) -- le reste du robot, y compris les positions ouvertes,
# n'en sait rien et continue normalement. Installer seulement si les
# notifications push doivent fonctionner :
#
#   pip install "PyJWT[crypto]"
PyJWT[crypto]==2.14.0
